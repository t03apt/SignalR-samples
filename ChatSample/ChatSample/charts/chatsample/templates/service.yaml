apiVersion: v1
kind: Service
metadata:
  name: {{ template "chatsample.fullname" . }}
  labels:
    app: {{ template "chatsample.name" . }}
    chart: {{ template "chatsample.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "chatsample.name" . }}
    release: {{ .Release.Name }}
